# ROADMAP.md — 実装ステップと完了条件

**一度に全部作らない。** ステップごとに実装→デプロイ→実機（娘さんの端末）で確認→次へ。
各タスクは1PR目安。完了条件（Acceptance Criteria, AC）を満たさないPRはマージしない。

---

## ステップ0：土台づくり ✅

**目的**：動くPWAの骨組みとデータ層を用意する。

- [x] Vite + React + TypeScript 初期化
- [x] vite-plugin-pwa 設定（manifest・アイコン・オフラインキャッシュ）
- [x] GitHub Pages / Netlify へのデプロイ設定（push で自動デプロイ）
- [x] `src/types.ts`・`src/data/*.ts` の雛形（DATA_MODEL 準拠）
- [x] `lib/storage.ts`：SaveData の load/save/初期化、schemaVersion

**AC**：空のアプリがデプロイURLで開け、ホーム画面に追加でき、再読み込みで保存が残る。

---

## ステップ1：ミッション＆スタンプ（実用最小限）✅

**目的**：これだけで紙版の代わりに毎日使える状態にする。

- [x] プロフィール選択（姉/妹）と切り替え、前回選択の記憶
- [x] ホーム：アバター（仮）・日付曜日（自動）・今日のミッション一覧
- [x] ミッションタップ → スタンプ選択パレット → ペタッと押す（アニメ）
- [x] 長押しで取り消し
- [x] 「じぶんできめたミッション」の内容選択/入力
- [x] 当日データの保存（dailyRecords）
- [x] 「ぜんぶできた！」表示（演出・ガチャはステップ2）

**AC**：娘さん2人が各自、今日のミッションにスタンプを押せて、翌日も前日分が壊れず残る。

---

## ステップ1.5：週間・月間ビュー＋リカバリー＋月間ごほうび ✅

**目的**：習慣化を支える「振り返り」「リカバリー」「長期ごほうび」を追加する。
詳細仕様は `docs/STEP_1_5_SPEC.md` を参照。

- [x] タブ切り替え：きょう / こんしゅう / こんげつ
- [x] 週間ビュー（曜日×ミッション表、達成率、前の週/次の週ナビ）
- [x] 月間ビュー（カレンダー色分け、日タップ詳細、前の月/次の月ナビ）
- [x] ミッション別達成率・「にがて」マーク
- [x] リカバリーミッション（「あと1個」判定、週上限、銀の花丸）
- [x] 月間ごほうび（しきい値達成で報酬、二重付与防止）
- [x] おうちの人モード（PINロック＋リカバリー設定・月間設定）
- [x] schemaVersion 2 へのマイグレーション（既存データを壊さない）

**AC**：STEP_1_5_SPEC.md §8 参照。

---

## ステップ2-A：ガチャ基本の流れ ✅

**目的**：ガチャの抽選・演出・景品付与の基本フローを完成させる。
詳細仕様は `docs/STEP_2_GACHA_SPEC.md` を参照。

- [x] `lib/gacha.ts`：7段階レア度抽選・景品抽選・ダブり→かけら変換・PullType定義
- [x] `data/gachaVisuals.ts`：レア度別ビジュアル定義（カプセル色・グロー・レベル）
- [x] `lib/sounds.ts`：Web Audio API によるレア度別効果音
- [x] ガチャ画面（Gacha.tsx）：シルエット→マシン→カプセル→景品の演出フロー
- [x] スキップ（タップで結果へ）
- [x] レア度別の見た目差（カプセル色・グロー・効果音・フィニッシュ演出）
- [x] 景品付与（ポイント/スタンプ/アイテム）と結果表示
- [x] ダブりはかけらに変換して表示
- [x] デイリー（コンプリート）・リカバリー・スペシャルガチャ券 3種の引き口
- [x] 重複防止（gachaPulled / specialGachaTickets）

**AC**：STEP_2_GACHA_SPEC.md §8「2-A」参照。

---

## ステップ2-B：期待度演出（ドキドキを乗せる）

**目的**：2-A の上に、煽り・リーチ・ガセ・大逆転などのシナリオを追加する。
詳細仕様は `docs/STEP_2_GACHA_SPEC.md` §2〜§5 参照。

- [ ] 演出シナリオデータ（`data/gachaScenarios.ts`）：レア度別シナリオ定義
- [ ] 「隠して大逆転」シナリオ（高レアなのに途中まで地味 → 最後にドーン）
- [ ] 「ガセ（フェイント）」シナリオ（低レアなのに途中まで派手 → 普通に終わる）
- [ ] 「確定演出」（ごく稀に出現、特定のレア度を約束）
- [ ] シナリオの重み付き選択エンジン（`lib/gachaPlayer.ts`）
- [ ] ダイヤモンド専用ファンファーレ演出
- [ ] 全体の長さ調整（通常5〜7秒、大当たり8〜10秒）

**AC**：STEP_2_GACHA_SPEC.md §8「2-B」参照。

---

## ステップ3：コレクション＆着せ替え

**目的**：「外れてもコレクションになる」satisfaction を作る。

- [ ] スタンプ図鑑（未所持はシルエット、取得率表示）
- [ ] アイテム図鑑（カテゴリ別、未所持シルエット）
- [ ] アバター着せ替え画面（カテゴリ選択→プレビュー→保存→ホーム反映）
- [ ] こうかんじょ（かけら交換所）：かけらで欲しいものと交換

**AC**：ガチャで得たアイテムでアバターを着せ替えでき、ダブりのかけらで狙ったアイテムを交換できる。

---

## ステップ4：週間ボーナス＆おうちの人モード（完全版）

**目的**：週次の達成感と、保護者の管理機能。

- [ ] 週（月〜日）コンプリート日数カウント＋進捗メーター
- [ ] 5日達成で週間ボーナス（演出＋報酬）、週またぎリセット
- [ ] ポイント自動集計（今週分・累計）
- [ ] おうちの人モード完全版（PINハッシュ化・ミッション編集・換算レート/上限設定・精算・export/import・リセット）
- [ ] バージョン表示

**AC**：週5日でボーナスが出て翌週リセットされる。おうちの人モードで設定変更・精算・バックアップができ、子どもは管理機能に触れない。

---

## ステップ5以降（運用・拡張）

- [ ] スタンプ・アイテムの追加（data配列に足すだけ）を定期的に
- [ ] 季節イベント限定ガチャ（期間限定プール）
- [ ] 効果音・アニメのブラッシュアップ
- [ ] （任意）ごほうびメーターやスタンプコレクションの演出強化

---

## レビュー観点（Claude＝レビュー担当）

- コンテンツ分離が守られているか（画面に個別アイテムをハードコードしていないか）
- データ消失リスクがないか（書き込み一貫性・スキーマ・二重付与防止）
- 子ども向けUX（ボタンサイズ・誤操作防止・オフライン）
- ガチャ確率が定義どおりか（簡易な分布テストがあると尚良い）
- 1PR1タスク・ACを満たしているか
